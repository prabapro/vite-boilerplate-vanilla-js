/* eslint-disable no-console */
console.log('sample file can be hosted in a cdn like jsdelivr.');

// Example function 1: Add two numbers and return the result
function addNumbers(a, b) {
  return a + b;
}

// Example function 2: Check if a number is even
function isEven(number) {
  return number % 2 === 0;
}

// Example usage:
console.log(addNumbers(5, 3)); // Output: 8
console.log(isEven(6)); // Output: true
